# radicalred-savehack

This repository is a standalone GitHub Pages build of the Radical Red save editor.

The `runtime/` directory contains the data and parser scripts required by the app. Deploy the repository root as the Pages source and open `/index.html`; no parent repository files are required.
